<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Todos</title>
</head>

<body>
    <nav>
        <a href="/todo">Todo List</a>
    </nav>
    <h1>Todo <%= (todo.id)? 'Edit' : 'Add' %> </h1>
    <form action="/todo/save" method="post">
        <input name="id" type="hidden" value="<%=todo.id %>" />
        <input name="txt" type="text" value="<%= todo.txt%>"  />
        <label>
            ||  Done:
            <input name="completed" value="true" type="checkbox"
            <%= (todo.completed)? 'checked' : '' %>   />
        </label>
        <label>
            || Importance:
            <input
                list="importance" id="importance-chosen" name="importance"
                value="<%=todo.importance%>"
            />
            <datalist id="importance">
                <option value="1">
                <option value="2">
                <option value="3">
            </datalist>
        </label>
        <label>
            || Category:
            <input
                list="category" id="category-chosen" name="category"
                value="<%=todo.category%>"
            />
            <datalist id="category">
                <option value="Work">
                <option value="Life Style">
                <option value="Family">
            </datalist>
        </label>
        <label>
            || Color:
            <input type="color" id="color-choise" name="color"
                value="<%= (todo.color)? todo.color : "#a76465" %>"
            />
        </label>
        <button type="submit">Save</button>
    </form>

</body>

</html>